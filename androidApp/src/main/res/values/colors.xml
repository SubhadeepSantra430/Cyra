<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only kept here because the adaptive-icon/splash-screen XML APIs need a plain color
      resource, not a Compose value. Everything else in the app should use CyraTheme's
      MaterialTheme.colorScheme (see ui/theme/Color.kt) - this is the one XML-only exception.
    -->
    <color name="ic_launcher_background">#FFFFFF</color>
</resources>
